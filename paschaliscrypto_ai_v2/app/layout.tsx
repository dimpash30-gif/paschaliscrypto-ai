import './globals.css'
export const metadata = { title: 'PaschalisCrypto AI', description: 'Crypto signals dashboard' }
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return <html lang="el"><body>{children}</body></html>
}
